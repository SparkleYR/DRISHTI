VITE_API_BASE_URL=http://127.0.0.1:8000
# The global stream requested by the dashboard is not part of the current backend contract.
# For live target telemetry, set this to an active session endpoint instead:
# ws://127.0.0.1:8000/api/v1/walk/sessions/<session_id>/telemetry
VITE_STREAM_WS_URL=ws://localhost:8000/ws/stream
